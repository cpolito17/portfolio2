/* The resume, for variants that render it in-page rather than sending the
   visitor to /about. Same JSON the standalone page is generated from, so the
   two cannot drift. */
const RESUME = __RESUME__;
